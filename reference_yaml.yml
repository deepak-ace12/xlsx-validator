# Every class should be of the below form:
# ClassName:
#   - must have field 1
#   - must have field 2
#   - ...
#   - ...
#   - must have field n


RequiredValidator:
  - error_msg

RegexValidator:
  - pattern
  - error_msg
  - full_match

LengthValidator:
  - operation
  - threshold
  - error_msg
  - trim

OptionValidator:
  - options
  - error_msg
  - case_sensitive

EmailValidator:
  - error_msg
  - trim

TypeValidator:
  - type
  - error_msg

DatetimeValidator:
  - format
  - error_msg

DateValidator:
  - format
  - error_msg

ExcelDateValidator:
  - error_msg

ComparatorValidator:
  - operation
  - threshold
  - error_msg

NonNegativeValidator:
  - type
  - error_msg

high_level_keys:
  - exclude
  - iterate_by_header_name
  - validations
  - must_have_columns
